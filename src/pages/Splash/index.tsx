import React from 'react'

import './index.scss';

const Splash: React.FC = ({}) => {

	return (
		<section id={'splash-page'}>
			<div>
				<h2>Hello World!</h2>
			</div>
		</section>
	);
}

export default Splash;
